  <div class="bg-gradient-to-r from-blue-600 via-indigo-600 to-purple-600 py-24 px-6">

    <div class="max-w-[1152px] mx-auto bg-[#0b122a] rounded-3xl shadow-2xl text-center py-14 px-3 md:px-8">
      <h2 class="text-4xl md:text-5xl font-semibold text-white!">
        {{ section.settings.join }}
      </h2>

      <p class="text-gray-300 mt-4 text-lg">
        {{ section.settings.join_message }}
      </p>
      <div class="mt-8">
        <button
          class="bg-yellow-500 hover:bg-yellow-400 text-white font-medium px-10 py-3 rounded-full transition duration-300 hover:text-black transition">
          {{ section.settings.join-button }}
        </button>
      </div>

    </div>

</div>

{% schema %}
{
  "name": "Join Our Tribe",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "join",
      "label": "Heading",
      "default": "Become Part of the Story"
    },
    {
      "type": "inline_richtext",
      "id": "join_message",
      "label": "Join Our Tribe Message"
    },
    {
      "type": "text",
      "id": "join-button",
      "label": "Join Button Text",
      "default": "Join Our Tribe"
    }
  ],
    "presets": [
        {
        "name": "Join Our Tribe"
        }
    ]
}
{% endschema %}